apiVersion: v1
kind: Pod
metadata:
  name: pod-secret
  #namespace: roboshop
spec:
  containers:
    - name: nginx
      image: nginx:latest
      envFrom:
        - secretRef:
            name: secrets
