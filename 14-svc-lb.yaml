apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  type: LoadBalancer
  selector:
    project: roboshop
    environment: dev
    kubernetes: "true"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
