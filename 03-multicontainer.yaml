apiVersion: v1
kind: Pod
metadata:
  name: multi-container
  namespace: roboshop
spec:
  containers:
    - name: nginx
      image: nginx:latest
    - name: almalinux
      image: almalinux:9
      command: ["sleep", "2000"]
