apiVersion: v1
kind: Pod
metadata:
  name: nginx-service
  labels:
    project: roboshop
    environment: dev
    kubernetes: "true"
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80
